//An enum variable is a special data type that allows you to define a set of named constants.
; //put const before the enum for getting the more optimized js code
var mySize = 2 /* Size.Medium */;
console.log(mySize);
