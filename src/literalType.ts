//Literal means exact value or secific value

type Qunatity = 50 | 100; //this is union type and this type defined is custom type and this is called literal type which has exact value or specific value

let quantity: Qunatity = 50; //here we can only assign 50 or 100 to quantity variable 

type Metric = '50' | '100'; //this is union type and this type defined is custom type