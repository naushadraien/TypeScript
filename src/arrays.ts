let numbers: number[] = [1, 2, 3, 4, '5']; //here 5 is not a number it is a string so it is showing the red line below the number 5

numbers.forEach(n=> n.) // this will show the options in type script when we type . after anything